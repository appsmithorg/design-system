import { CLASS_NAME_PREFIX, TEST_ID_PREFIX } from "__config__/constants";

export const ToggleClassName = CLASS_NAME_PREFIX + "-toggle-button";
export const ToggleIconClassName = ToggleClassName + "__icon";

export const ToggleButtonTestID = TEST_ID_PREFIX + "-toggle-button";
