import React from "react";
import { ComponentMeta, ComponentStory } from "@storybook/react";

import { {{capitalize name}} as {{capitalize name}}Component } from "./{{capitalize name}}";

export default {
  title: "Design System V2/{{capitalize name}}",
  component: {{capitalize name}}Component,
} as ComponentMeta<typeof {{capitalize name}}Component>;

// eslint-disable-next-line react/function-component-definition
const Template: ComponentStory<typeof {{capitalize name}}Component> = (args) => {
  return <{{capitalize name}}Component {...args} />;
};

export const {{capitalize name}} = Template.bind({});

{{capitalize name}}.args = {};
